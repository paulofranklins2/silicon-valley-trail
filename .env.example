# Silicon Valley Trail
#
# No API keys required. (keyless)
# Uses Open-Meteo (weather) and OSRM (routing).
#
# Weather mode (application.yml):
#   api   real weather
#   mock  random weather
#   demo  cycle all types